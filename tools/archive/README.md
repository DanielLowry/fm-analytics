# Archived research scripts

Files here preserve one-off experiments whose conclusions remain useful but
which must not be offered as current commands.

- `fm20_interested_filter_probe.py` faulted at its first native filter call.
  The immutable report is indexed as `interested-filter-fault` in
  `research/corpus.json`; rerunning the script would repeat a known unsafe ABI
  attempt.

